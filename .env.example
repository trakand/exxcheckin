# Copy this file to .env.local for local development.
# If empty, the app runs in mock mode and does not write to Google Sheets.
VITE_API_BASE_URL=

# Set true to force mock mode even if API URL is present.
VITE_USE_MOCK_API=false
